# PS Editor - OCR + Inpainting prototipi bağımlılıkları (adım 2 ve 3)
#
# Balon/metin bölgesi tespiti: ogkalu/comic-text-and-bubble-detector
#   (Apache-2.0, RT-DETR-v2; ONNX model ilk çalıştırmada Hugging Face'ten iner)
# OCR: manga-ocr (Apache-2.0, PyPI 0.1.14, saf Python wheel)
# Inpainting: simple-lama-inpainting (Apache-2.0, PyPI 0.1.0; big-lama
#   checkpoint ~206 MB ilk çalıştırmada GitHub release'den iner).
#
# Notlar:
# - transformers <5 zorunludur: manga-ocr 0.1.14, transformers 5.x ile
#   tokenizer yüklemesinde bozulur (yukarı akışta düzeltildi, burada pin'lenmiştir).
# - torch: Windows PyPI wheel'i CUDA desteklidir (RTX 4060 otomatik bulunur).
#   Açık bir CUDA sürümü kurmak isterseniz:
#   pip install torch --index-url https://download.pytorch.org/whl/cu126
# - onnxruntime yalnızca CPU içindir (tespit modeli CPU'da çalışır; ağır
#   yük olan OCR, PyTorch üzerinden GPU'ya gider).
# - opencv (cv2) ayrıca listelenmez: simple-lama-inpainting onu bağımlılık
#   olarak çeker. opencv-python ile opencv-python-headless birbiriyle
#   çakışır; ikisini aynı anda elle kurmayın.

numpy
pillow>=10.0.0
onnxruntime
huggingface_hub[hf_xet]
torch
torchvision
manga-ocr==0.1.14
transformers>=4.25,<5
simple-lama-inpainting==0.1.0

# Adim 4: LLM ceviri + typesetting
#   openai    : OpenAI-uyumlu ucnokta istemcisi (Apache-2.0)
#   anthropic : Anthropic Claude Messages API istemcisi (Apache-2.0)
#   python-dotenv : .env dosyasindan API anahtari okuma (dev fallback)
python-dotenv>=1.0
openai>=1.40
anthropic>=0.40

# Adim 5: OS guvenli anahtar deposu (Windows Credential Manager /
# macOS Keychain / Linux Secret Service). Cagri platforma gore keyring'in
# kendi platform ozel bagimliliklarini kurar (Win: pywin32-ctypes,
# Linux: SecretStorage + jeepney). PyInstaller paketleme notu:
# python-sidecar.spec icindeki hiddenimports'a bakin.
keyring>=25.0,<26
